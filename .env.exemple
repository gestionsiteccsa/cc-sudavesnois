# ============================================
# Configuration générale
# ============================================

# Clé secrète pour la sécurité (changer en production)
SECRET_KEY=django-insecure-#x9&kq@#v8m^2n$p5r*7s!4t%6u&8w@1y#3z$5b^7n*9m!2q@4s#6f&8h

# Mode debug (True pour développement, False pour production)
DEBUG=True

# Hôtes autorisés (séparés par des virgules)
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# Paramètres de sécurité
# ============================================

# Utiliser HTTPS en production
SECURE_SSL_REDIRECT=False

# En-têtes de sécurité
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Cookies sécurisés
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# Protection XSS
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True

# ============================================
# Configuration Email (SMTP)
# ============================================

# Backend email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# Configuration SMTP
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False

# Identifiants email
EMAIL_HOST_USER=votre-email@gmail.com
EMAIL_HOST_PASSWORD=votre-mot-de-passe-app

# Email par défaut
DEFAULT_FROM_EMAIL=noreply@votresite.com
SERVER_EMAIL=admin@votresite.com
